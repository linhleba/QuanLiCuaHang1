@model IEnumerable<QuanLiCuaHang.Areas.Manager.Data.TINHTRANGDV>

@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/Manager/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TenTinhTrang)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TenTinhTrang)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MaTinhTrangDV }) |
            @Html.ActionLink("Details", "Details", new { id=item.MaTinhTrangDV }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MaTinhTrangDV })
        </td>
    </tr>
}

</table>
